import { createServer } from '../server.js';

/**
 * This file acts as the entry point for Vercel's serverless functions.
 * It imports and exports your existing Express app, allowing Vercel to
 * run it as a single serverless function that handles all API requests.
 */
export default createServer();
